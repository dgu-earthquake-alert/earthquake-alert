version: "3.8"
services:
  frontend:
    build: ./earthquake-alert
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production

  backend:
    build: ./earthquake-alert-spring
    ports:
      - 8080:8080
    environment:
      - SPRING_PROFILES_ACTIVE=docker
